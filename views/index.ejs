<!DOCTYPE html>
<html>
  <head>
    <meta chatset="utf-8">
    <title><%= title %></title>
  </head>
  <body>
    <% if (session.user) { %>
      <h2>Bienvenido a <%= title %>, <%= session.user.username %> </h2>
    <% } else { %>
    <h2>Bienvenido a <%= title %> </h2>
    <% } %>
    <p>El portal donde podrá crear sus propios juegos</p>
  </body>
</html>
